#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include <ctype.h>
/**
 * main  - Prints whether the last digit of a random number is greater than 5
 * Return: 0
 */

int main(void)
{
	int x;
	int t 
	for(t = 'a'; x <= 'z'; t++)
	{
		putchar(t);
	}
	for(x = 'A'; x <= 'Z'; x++)
	{
		putchar(x);
	}
	putchar('\n');
	return 0;
}
